<% form_with url: "/competitions/#{@competition.id}", method: :patch do |f| %>
  <%= f.label :nickname %>
  <%= f.text_area :nickname %>
  <%= f.label :hometown %>
  <%= f.text_area :hometown %>
  <%= f.submit 'Submit' %>
<% end %>
